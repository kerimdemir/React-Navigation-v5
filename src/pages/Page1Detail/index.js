import React, {useEffect} from 'react';
import {View, Text} from 'react-native';
import styles from './styles';

const Page1Detail = () => {
  useEffect(() => {}, []);

  return (
    <View style={styles.container}>
      <Text> Page1 Detail </Text>
    </View>
  );
};

export default Page1Detail;
